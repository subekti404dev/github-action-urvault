{
  "name": "github-action-urvault",
  "version": "1.0.0",
  "description": "",
  "main": "index.cjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx esbuild index.js --bundle --outfile=index.cjs --format=cjs --platform=node"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.9.0",
    "axios": "^0.27.2"
  }
}
